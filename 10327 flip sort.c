#include<stdio.h>

int main()
{
    int n,num,i,flip;
   while(scanf("%d",&n)!=EOF)
    {
       for(i=1;i<=n;i++)
       {
           scanf("%d",&num);
           printf("Minimum exchange operations : %d",flip);

       }
    }

    return 0;
}
